export const BATCH_SIZE = 10;
export const DELAY_BETWEEN_BATCHES = 100; // in milliseconds

// FIX: Define and export the GEMINI_MODEL constant to resolve the import error in services/geminiService.ts.
export const GEMINI_MODEL = 'gemini-2.5-flash';
